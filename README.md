# Chaldea Bond

《Fate/Grand Order》阵容羁绊礼装分析工具。用户选择五名自有英灵与一名助战后，系统会根据关卡基础羁绊、助战礼装规则和可用礼装库存，计算整队总羁绊最高的装备方案，并解释每个推荐。

> 当前版本是可运行的 Web MVP。游戏数据与规则可能随服务器版本更新，请以游戏内说明为准。

## 核心能力

- 六槽位阵容编成：五名自有英灵 + 一名助战
- 已选择卡槽支持按住拖拽并与目标槽直接交换，助战身份随卡槽移动
- 441 名国服英灵的名称、职阶和图鉴编号搜索
- 满破羁绊礼装库存配置
- 八周年常驻首发成员羁绊 `+20%` 规则
- 百分比与固定值收益的精确比较
- 助战位「迦勒底午茶时光」15% 特殊规则
- 助战可与任意槽位换位，并计算前排助战触发的全队 `+4%`
- 每槽位推荐、收益解释和整队汇总
- 每名英灵展示礼装乘区、站位乘区和固定羁绊三步算式
- 响应式布局，支持手机与桌面浏览器

## 技术架构

```text
src/
├── components/       # 纯 UI 组件
├── data/             # 版本化的英灵与礼装数据
├── domain/           # 与框架无关的羁绊规则、优化器和类型
└── lib/              # 日志等横切能力
```

技术栈为 React 19、TypeScript、Vite、Vitest 与 ESLint。领域层不依赖 React，可在后续微信小程序项目中直接复用，或独立发布为共享 npm workspace package。

优化器当前使用穷举的一对一分配。阵容固定最多六槽、礼装池规模很小，这种实现比贪心策略更易审计，并可正确处理助战专属数值与固定加成。

## 本地开发

要求 Node.js 20 或更高版本、pnpm 11。

```bash
pnpm install
pnpm dev
```

质量检查：

```bash
pnpm lint
pnpm test
pnpm build
```

## 数据更新

英灵基础数据来源于 [Atlas Academy](https://atlasacademy.io/) 的公开游戏数据导出。下载国服 `basic_servant.json` 后执行：

```bash
node scripts/generate-servants.mjs \
  /path/to/basic_servant.json \
  src/data/servants.json
```

生成器只保留产品需要的字段，避免把上游大型数据文件直接打包进前端。

## 当前计算边界

- 按常驻关卡结算规则计算，前三个首发槽位中的自有英灵额外获得 20% 羁绊。
- 基础公式为：
  1. `floor(基础羁绊 × (1 + 礼装百分比 + 活动百分比))`
  2. 对第一步结果应用站位乘区并再次向下取整
  3. 最后加入英灵肖像等固定羁绊
- 助战在首发时，其首发 20% 平摊为五名自有英灵各 4%；首发自有英灵因此使用 24%，后备使用 4%。
- 活动百分比字段当前保留为 0%，尚未接入活动规则。
- 羁绊礼装视为满破且同名礼装仅使用一张。
- 助战英灵不计入玩家获得的羁绊，但其礼装对自有成员生效。
- 玛修按无法通过普通关卡获得羁绊处理，其礼装仍能为全队加成。
- 暂未纳入活动或主线期间按职阶、性别、阵营等特性提供的限时羁绊加成。

## 后续路线

1. 建立带版本号的活动规则数据，支持关卡与限时特性羁绊加成。
2. 保存玩家礼装库存、常用阵容和英灵羁绊上限状态。
3. 将 `domain` 与 `data` 拆为 workspace 共享包，接入 Taro 或原生微信小程序。
4. 增加数据更新流水线、离线缓存、可观测性与错误上报。

## 版权声明

本项目为非官方玩家工具，与 TYPE-MOON、Lasengle 或 FGO PROJECT 无隶属关系。游戏名称、图像和相关素材版权归原权利人所有。
